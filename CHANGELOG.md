## 1.0.3

- Updated LICENSE format to standard OSI MIT structure and matched standard OSI MIT template for pub.dev recognition.
- Added comprehensive dartdoc documentation across all public classes and members.

## 1.0.2

- **Breaking Change**: Transitioned to a completely headless builder architecture using `FormikField`.
- Added support for dynamic key-path resolution (`user.profile.name`, `users.0.name`).

## 1.0.1

- Added support for dynamic form arrays (repeatable container fields).
- Integrated dual-level validation engine supporting both field-level and form-level schema validation.
- Added automatic `TextEditingController` state sync inside `FormikField`.
