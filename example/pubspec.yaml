name: example
description: Demonstrating how to use formik_flutter.
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  formik_flutter:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
